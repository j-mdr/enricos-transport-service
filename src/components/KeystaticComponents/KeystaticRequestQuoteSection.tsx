export default function KeystaticRequestQuoteSection() {
  return (
    <div
      style={{
        border: "2px dashed #ccc",
        padding: "1rem",
        borderRadius: "8px",
        textAlign: "center",
      }}
    >
      <strong>Offerteformulier (RequestQuote)</strong>
      <p style={{ color: "#666", marginTop: "0.5rem", fontSize: "0.875rem" }}>
        Labels worden beheerd via Globale instellingen → Offerte labels
      </p>
    </div>
  );
}
