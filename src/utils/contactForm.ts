import type { Locale } from "@config/siteSettings.json";

// Labels komen straks uit Sanity form documents.
// Components hebben ingebouwde fallback labels.
export async function getContactFormLabels(_locale: Locale) {
  return null;
}
